#include <stdio.h>
/**
 * main - this program prints all digits number of base 10 starting from 0
 * Return: 0 indicates success
 */
int main(void)
{
	for (int i = 0; i < 10; i++)
		printf("%d\n", i);
	return (0);
}
